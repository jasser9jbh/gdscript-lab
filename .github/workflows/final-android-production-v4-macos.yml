name: GDScript Lab Android production v4 macOS (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-22.04
    steps:
      - run: |
          echo 'Retired: this workflow used an ephemeral Android signing identity.'
          echo 'Use final-android-production-v5-windows.yml with the persistent owner-controlled Android keystore supplied through GitHub Actions secrets.'
