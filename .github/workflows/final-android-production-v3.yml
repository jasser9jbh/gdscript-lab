name: GDScript Lab Android production v3 (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-22.04
    steps:
      - run: |
          echo 'Retired: this workflow generated and uploaded a new private Android signing identity.'
          echo 'Use final-android-production-v5-windows.yml, which accepts only a persistent owner-controlled keystore from GitHub Actions secrets and never uploads it.'
